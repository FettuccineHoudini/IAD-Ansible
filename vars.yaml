# This is where our Ansible variables are going to go so we can 
# ssh into the hosts in our inventory
---
ansible_port: 22 # SSH port
ansible_user: ec2-user # EC2 Username
ansible_ssh_private_key_file: ./IAD_Ansible_key.pem # Path to my private key